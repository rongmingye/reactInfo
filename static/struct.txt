实习信息采集系统 info
主要功能：
	老师查看学生的实习信息
	学生填写实习信息

表
	info
		id  student_id student_name teacher_name teacher_tel practice_time practice_type practice_long practice_company relation_name relation_tel  arrange post salary company_taken tenBreak sixteenBreak changed remark

	student
		id student_id student_name student_pwd student_tel school academe grade  profession profession_id class_name

	teacher
		id  teacher_name teacher_pwd teacher_id teacher_tel 

	recruit
		id company post address required salary eatLive

模块
	
	登陆 
		1 老师登陆 2 学生登陆
		提交usrname pwd userType, 查询teacer表有没有该用户

	老师页面
		左侧老师信息
			查询teacher表： 展示老师信息
			修改密码

			发布招聘信息： 跳到发布招聘页面
			查看招聘信息：跳到查看招聘页面，直接展示招聘内容

		右侧
			学生的信息
			学生的实习信息
			

	学生页面
		左侧学生信息
			查询student表：展示学生的信息
			修改密码
			
			查看招聘信息：跳到查看招聘页面，直接展示招聘内容

			查看公告

		右侧
			展示实习信息
			修改实习信息

用到的技术
	响应式布局 @media
	create-react-app
	react
		this.state  this.setState jsx ref 事件 表单 defaultValue value
	react-router-dom
		history 
		BrowserRouter Switch Route Link
	react-redux
		action reducer store
	cookie jquery-cookie
		$.cookie(key,value,obj)
	express
		var app = express();
		app.use(exprss.static(__dirname +  "" ));
		app.listen(port, ip, callback);
	mysql
		select * from tb1 inner join tb2 on tb1.key1 = tb2.key1 where key1 = 1;

skill
	事件触发 onClick={()=>{ }}

	input的默认值是defaultValue，可以修改的
		如果是value={this.state.data}是，不能修改的

	刷新问题：不要滥用redux, 页面刷新的store被重置了

	react页面跳转：1 this.props.history.push(path, state)
				   2 <Link  to={{pathname:"", state: "",}}/>
	获取location.state： this.prop.history.location.state

	jquery.cookie
		 import $ from jquery
		 import "jquery.cookie"

		$.cookie(key,value,obj)
		添加cookie: $.cookie("isLogin", true);
		获取cookie: $.cookie("isLogin");
		删除cookie: $.cookie("isLogin", null);

	注意阿里云的安全组的端口
